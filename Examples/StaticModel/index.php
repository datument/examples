<?php

declare( strict_types= 1 );

namespace Datument\Examples\StaticModel;

////////////////////////////////////////////////////////////////

/**
 * To static model, data are just stored within code.
 * It's fit for storing meta data with a small amount and few changing,
 * provide fast accessibility and same behaviors with normal models.
 * You should update data via updating code, runtime creating, updating, deleting are denied.
 */
